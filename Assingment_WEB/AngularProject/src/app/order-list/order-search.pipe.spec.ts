import { TeamSearchPipe } from './order-search.pipe';

describe('TeamSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
